# Maple 숙제 매크로 실행 기록

## 숙제 맨 마지막 단계: 엘리트 몬스터 연속 소환 (2026-09-19 최신 사용자 지시)

- 모든 계정에서 다른 숙제와 이벤트 보상 수령을 마친 뒤, **엘리트 몬스터 연속 소환 활성화를 마지막 게임 조작 단계로 반드시 진행**한다.
- 필드 하단 **캐릭터 바로 왼쪽의 동그란 버튼**은 엘리트 몬스터 소환 버튼이다. **그 동그란 버튼의 왼쪽에 있는 반복 버튼**을 눌러야 연속 소환이 켜진다. 두 버튼을 혼동하지 않는다.
- 각 계정에서 현재 반복 상태를 먼저 확인하고 꺼져 있으면 반복 버튼을 눌러 켠다. 이미 켜진 상태를 다시 눌러 끄지 않는다. 실제 화면에서 필요한 확인창을 확인하고 처리한다.
- 일반 일일 미션 **15/15 달성 및 보상 수령과 별개의 마무리 작업**이다. 15/15 또는 오늘 소환 완료 기록을 이유로 마지막 소환을 생략하지 않는다.
- 단발 소환이나 확인창 닫기만으로 완료 처리하지 않는다. 반복 버튼의 활성 표시와 실제 연속 소환 동작을 확인하고 기록한다. 자동 전투로 처치될 수 있으므로 몬스터 한 마리가 계속 남아 있는지를 완료 기준으로 삼지 않는다.
- 소환 불가·확인 실패는 마지막 소환 미완료로 보고한다. 마무리 소환 후에는 다른 숙제 메뉴로 이동하지 않고 결과 확인·캡처만 한다.
- 이 단계는 기존 `full_flow_runner.py`의 이벤트 종료 후 자동 실행에 아직 연결되어 있지 않으므로, 해당 러너 사용 시에도 수동으로 마지막 소환을 수행하고 검증해야 한다.

## 엘리트 몬스터 소환 버튼 위치 (2026-09-19 사용자 확인)

- 화면 하단의 **캐릭터 버튼 바로 왼쪽에 있는 동그란 버튼**이 엘리트 몬스터 소환 버튼이다.
- 실행 시 현재 화면에서 이 버튼을 확인하고 누른다. 창 크기나 배치가 바뀌면 기존 좌표만으로 판단하지 않는다.
- 소환 확인창을 확인한 뒤 실행하고, 완료 여부는 일반 일일 미션의 엘리트 소환 **15/15** 및 보상 수령으로 검증한다.

## 최신 사용자 지시 (2026-09-18)

- 엘리트 소환 최종 검증은 **일반 일일 미션 15/15**까지 확인한다. 핑크빈 10/10과 일일 미션 종합 8/8만으로 엘리트 소환 숙제 완료로 판단하지 않는다. 9/18 저녁 재검증에서 Air1의 10/15 누락을 발견했다. 추가 소환 후 개별 엘리트 미션 보상을 수령한다.

- 릴레이는 스탬프 사용 후 다음 라운드 버튼이 실제로 활성화되어 있으면 진행한다. 이전의 판 전체 완료 제한보다 이번 지시를 우선한다. 비활성 버튼은 진행 완료로 기록하지 않는다.
- 여정 보상은 현재 최고 단계(Max)를 우선하고, 동률이면 왼쪽에서 오른쪽 순서로 선택한다. 보상 확인창까지 수령하고 경험치 감소를 확인한다.
- 보상 선택으로 가속이 활성화되면 사용한다. 확률 안내 확인창의 가속하기를 누른 뒤 경험치 증가를 검증한다. Air는 2회, Air1은 1회 후 바로가기로 바뀌는 상태를 관찰했다. 바로가기는 추가 가속으로 취급하지 않는다.
- 9/18 관찰: Air1 빠른 사냥 광고 탭은 y=.65이며, 보상창 닫기는 (.485,.763)가 필요했다. 우편 진입 시 로얄 헤어·성형 공지가 뜨면 우상단 X로 닫고 우편 상태를 다시 확인한다.
- 성장던전의 날짜별 완료 기록만 믿지 않는다. 특히 시작 시 이벤트 창이 열려 있으면 먼저 닫고, 핑크빈 성장던전 미션 0/1과 완료 기록이 충돌하면 강제 재검증한다.

## 핑크빈 릴레이 스탬프 기준 (2026-09-12 사용자 지시)

- 현재 라운드의 **모든 칸을 찍은 뒤에만** 다음 라운드로 이동한다.
- 특별 보상을 모두 받았다는 안내만으로 이동하지 않는다. 일반 보상 칸도 모두 완료되어야 한다.
- 보유 스탬프가 0개라는 것은 오늘 사용할 스탬프가 없다는 뜻이며, 현재 판 전체 완료와 다르다.
- 미완료 칸이 있으면 먼저 해당 칸을 찍을 수 있는 실제 UI를 조사한다. 찍기 버튼이 회색이어도 다음 라운드로 넘어가지 않는다.
- 다음 라운드 이동 전 판 전체 완료를 화면으로 확인한다. 확인할 수 없으면 현재 라운드를 유지하고 미완료로 기록한다.

## 단계별 재검증 규칙 (2026-09-05)

이 규칙은 실기기 재검증 전의 운영 기준이다. 기존 모든 러너에 적용이 끝난 것은 아니다.

1. 한 인스턴스의 한 동작만 실행한다. 입력 전 화면 제목, 선택 탭, 대상 버튼을 확인한다.
2. 입력 후 목표 상태를 1초 간격으로 관찰하고 연속 2회 일치해야 다음 단계로 진행한다.
   기본 관찰 제한은 12초이며 광고·전투는 해당 작업의 별도 제한을 사용한다.
3. 화면 변화량은 보조 신호다. 보상은 수령 완료 문구, 횟수 감소, 보유량 증가 등
   실제 결과로 판정한다. 버튼 소실이나 프로세스 정상 종료만으로 완료 처리하지 않는다.
4. 제한 시간 초과 시 캡처를 남기고 중단한다. 현재 화면을 재확인하기 전에는
   구매·보상·소탕 입력을 반복하지 않는다. 안전한 화면 이동도 상태 확인 후 재시도한다.
5. 미확인 항목은 완료 기록을 갱신하지 않는다. 미수령·미달성·재화 사용 보류·검증 불가를
   구분해 보고한다. 월간 출석의 초록색 보상 버튼과 핑크빈 스탬프도 별도 확인한다.
6. 한 인스턴스에서 실제 결과를 확인한 뒤 다음 인스턴스로 진행한다.

`ui_guard.wait_for_state`와 이벤트 창 진입 확인에 우선 적용했다.
보상 수령 완료 판정 및 나머지 러너는 실화면 검증이 더 필요하다.

세 인스턴스의 윈도우 PID는 실행할 때 자동 탐색한다. 독립 실행은 Codex 없이도
가능하며, Python 3와 macOS 접근성·화면 기록 권한이 필요하다.

```bash
python3 full_flow_runner.py
```

전체 흐름의 마지막에는 각 VM 절전 화면을 해제하고 메인 필드 화면을 캡처한 뒤
한 통의 완료 메일에 세 이미지를 첨부한다. 수신 주소와 Gmail SMTP 인증정보는
환경변수로만 전달한다.

```bash
export MAPLE_REPORT_TO_EMAIL="받을주소@example.com"
export MAPLE_SMTP_USERNAME="발신Gmail주소@gmail.com"
export MAPLE_SMTP_APP_PASSWORD="Gmail 앱 비밀번호"
python3 full_flow_runner.py
```

최종 JSON의 `completion_email.status`가 `sent`인지 확인한다. 메일 설정이 없는
점검 실행은 `--no-completion-email`로 명시적으로 비활성화할 수 있다.

자동 탐색이 어려운 환경에서는 현재 PID를 직접 지정할 수 있다.

```bash
python3 full_flow_runner.py --pids <AIR_PID> <AIR1_PID> <AIR2_PID>
```

개별 액션은 `homework_runner.py`, `booster_runner.py`, `shop_runner.py`,
`summon_runner.py`, `friend_runner.py`, `journey_runner.py`, `claim_runner.py`, `guild_arena_worldboss_runner.py`를 PID와 함께 실행한다.

`shop_runner.py`는 단독 실행 시에도 절전 화면을 먼저 해제한다. 상점 아이콘과
좌측 탭 전환은 화면 변화로 확인하고, 무료보상 수령 후 첫 카드의 `AD` 배지가
예상대로 사라졌는지 검증한다. 검증에 실패하면 다음 좌표로 진행하지 않는다.

블루스택 창이 겹치거나 음수 좌표의 다른 데스크톱으로 이동하면 다음 명령으로
사용자 디스플레이(위쪽 모니터, y=-1440..0)에 재배치한다. Air/Air 1을
위쪽에, Air 2를 아래쪽에 둔다.

```bash
python3 arrange_bluestacks.py --pids 22112 59938 31126
```

`full_flow_runner.py`는 보상 수령 흐름 후 `summon_runner.py`를 실행해 `무기 소환`과
`동료 소환`의 무료 보상만 확인한다. 활성 무료보상은 결과 레이어를 닫은 뒤
이어가며, 소환권·보석을 사용하는 `일괄 소환`은 절대 누르지 않는다.

네 번째 액션(`booster_runner.py`)은 빠른사냥의 누적 무료 보상과 광고 무료 보상만
수령한다. `보석 구매` 탭의 500/750 보석 등 유료 수령은 실행 대상이 아니다.
2026-09-10에는 Air1의 탭 위치 차이와 배경 퀘스트 `1/1` 오인으로 수동 점검 중
500보석이 한 번 사용되었다. 실행을 중단하고 사용자에게 알린 뒤 아래 검증을 추가했다.

`fast_hunt_guard.py`는 빠른사냥 모달 제목과 해당 모달 내부의
`무료/광고 보상 남은 횟수`를 읽어 같은 상태를 두 번 확인한다. 배경 퀘스트는
판독 영역에서 제외한다. 광고 수령 전 유료 탭을 감지하면 즉시 중단하며, 무료
탭의 누적 횟수가 소진되어 유료 탭으로 바뀌면 무료 수령을 종료한다.
Air1 탭 좌표는 무료 `(0.350, 0.580)`, 광고 `(0.620, 0.580)`으로 확인했다.
화면 배치가 달라지면 현재 화면에서 다시 조사한다. 부스터와 반복사냥은 이어서
진행하며, 버닝 필드 아이템은 보유 수량이 확인된 경우에만 사용한다.

상점 보상(`shop_runner.py`)은 우측 상단 장바구니에서 `일반상점` 첫 카드의
`일간 1/1 무료 보상`을 1회 받는다. 첫 카드 우상단의 cyan `AD` 배지가 없으면 이미 보상 카드를 소진한
상태로 보고 해당 카드를 건너뛰므로
유료 주간/패키지 상품을 잘못 누르지 않는다. 전환 후 무료 버튼을 최대 3회 폴링하고,
클릭 입력은 실패 시 1회 재시도하며 보상 팝업 닫기 뒤에는 1.2초 대기한다.

## 추가·수정 숙제 (2026-09-09 실화면 조사)

친구 교환(`friend_runner.py`)은 `햄버거 메뉴 → 친구 → 모달 우측 하단`에서
`일괄 전달`을 한 번 누르고 보상 팝업을 닫은 다음, 같은 위치의 `모두 수령`을
한 번 누른다. 버튼 옆 포인트는 **지불 비용이 아니라 획득량**이다. 기존의
“유료 일괄 전달 금지” 및 “첫 친구의 개별 버튼만 클릭” 기준은 폐기한다.
보내기·받기는 각각 최대 1회, 보상 팝업 닫기는 별도 동작이다. 이미 보내기를
끝냈다면 현재 가능한 받기만 실행한다. 수량은 시간대와 친구 상태에 따라 달라져
고정값을 기대하지 않고, 전후 일일 포인트 증가와 실제 아이템 획득을 확인한다.
종료는 `주고받기 완료` 문구를 두 번 확인한다. `440/500`에서도 현재 교환이
끝날 수 있으므로 500 도달을 강제하지 않는다. 그날 완료 기록이 있어도 다음
숙제·재검증 시 화면을 다시 확인한다. `.friend_bulk_state.json`은 마지막 검사와
획득량을 기록할 뿐 실행을 막지 않는다. 기존 `.friend_state.json`은 새 러너의
건너뛰기 조건으로 사용하지 않는다.

여정의 보물(`journey_runner.py`)은 `햄버거 메뉴 → 용사의 여정 → 여정의 보물 →
상단 보물 경험치 게이지 우측 가속하기`를 숙제에 추가한다. 현재 활성 버튼만
실행하며 1회 후 경험치 증가를 두 번 확인하고, 아직 사용 가능하면 2회째까지
진행한다. 최대 2회이며 비활성 버튼을 반복 클릭하지 않는다. 새 확인창·구매창이
나오거나 경험치 증가를 검증하지 못하면 캡처를 남기고 중단한다.

- `20-10 클리어 시 개방`: 아직 잠긴 계정은 `locked`로 기록한다.
- 경험치 200%: `pending_reward_selection`으로 기록한다. 먼저 보상 카드 선택이
  필요하며 가속 완료로 처리하지 않는다. 카드 우선순위가 정해지기 전에는
  스타포스 주문서·큐브·주문의 흔적·메소 중 임의로 선택하지 않는다.
- 200% 미만인데 버튼이 회색: `unavailable`로 기록한다. 회색만으로 오늘
  가속 횟수를 이미 사용했다고 단정하지 않는다.
- 여정 화면을 닫은 뒤 햄버거 메뉴가 남으면 메뉴까지 닫고 필드 복귀를 확인한다.

실측 창 비율은 여정 메뉴 `(0.922, 0.317)`, 보물 탭 `(0.08, 0.411)`,
가속 `(0.760, 0.530)`, 친구 하단 일괄 버튼 `(0.762, 0.875)`이다.
창 제목줄을 포함한 비율이며 좌표만으로 실행하지 않고 제목·버튼·상태를 검증한다.
새 두 러너는 `screen_ocr.swift`를 로컬 Swift 컴파일러로 빌드하고 macOS Vision으로
OCR한다. `.run_audit/scheduled/`에 단계별 두 관찰 이상의 화면과 JSON을 저장한다.

실행 순서: `성장던전 → 무료/부스터/반복사냥 → 일반 상점 무료보상 → 무료 소환 →
친구 일괄 주고받기 → 여정의 보물 가속 → 길드/아레나/월드보스 → 우편함/패스/미션 → 이벤트 → 엘리트 몬스터 반복 버튼으로 연속 소환 활성화`.
여정 보상 선택 대기 또는 가속 잔여가 있으면 전체 결과에 `partial`을 표시한다.

조사 결과: Air 친구 500/500 완료, 가속 회색(EXP 68.2%);
Air1 친구 30→280(+250 전달)→440(+160 수령), 여정 EXP 200%로 선택 대기;
Air2 친구 30→100(+70 전달)→140(+40 수령), 여정은 20-10 미달로 잠김.
활성 가속을 실제로 눌러 경험치가 증가하는 경로는 이번 조사에서 검증하지 못했다.
다음 활성 상태에서 1회씩 확인하며 검증을 이어간다.

최종 소환 전 이벤트 수령(`event_runner.py`)은 폭죽 아이콘으로 이벤트 창을 연 뒤 현재
화면의 원형 빨간 알림점을 처리한다. 왼쪽 이벤트 목록은 스크롤을 지원하므로,
한 화면만 확인하고 끝내지 않고 목록을 위로 드래그한 다음 새 이벤트 본문을 다시
탐색한다. 두 화면 연속 새 이벤트가 없거나 목록이 더 움직이지 않으면 종료한다.
직사각형 `NEW` 라벨, 청록색 바로가기, 구매 버튼, 선택형 누적 보상은 자동 수령
대상에서 제외한다. 클라이언트 업데이트 직후에는 아래쪽 목록까지 수동으로 펼쳐
좌표와 제외 항목이 그대로인지 먼저 확인한다.
현재 핑크빈 일일 미션은 원형 알림점과 진행 막대가 붙은 주황색 `보상 받기`만
수령한다. `핑크빈 교환소`는 이벤트 재화를 소비하므로 자동화 대상이 아니다.

버튼 씹힘 방지를 위해 화면 전환 뒤 기본 대기시간은 1.0초로 설정했다. 성장던전
러너는 이제 각 클릭 전후의 해당 화면 영역을 캡처해 자연 발생 애니메이션 노이즈보다
큰 시각적 전환이 확인되지 않으면 재시도 후 즉시 중단한다. 마지막 결과 확인 뒤에는
성장던전 모달을 닫고 필드 복귀까지 확인하므로 다음 액션이 모달 위에 이어지지 않는다.
성장던전은 메뉴 전환 0.8초, 소탕 결과 확인 1.2초, `+` 연속 입력 0.25초를
사용하며, 보상 팝업 닫기 뒤에도 최소 0.8초를 둔다. 절전 해제 직후에는
1.2초 대기한다.

창이 다른 가상 데스크톱의 음수 `y` 좌표로 밀려 있으면 CGEvent 클릭이
성공 로그만 남기고 실제 게임에는 도달하지 않을 수 있다. `mac_gesture.py`는
이 경우 해당 PID의 BlueStacks 창만 `(50, 50)`으로 되돌린 뒤 클릭한다.

## 재실행 검증 메모

- Air 2(`31126`)는 재실행 시 무료 보상 탭과 부스터/길드 좌표가 Air와 달라
  인스턴스별 좌표 프로필을 적용했다.
- Air 2의 성장 던전 무기 소탕은 검증 당시 입장권이 0장이라 `재화가
  부족합니다`가 표시됐다. 이는 클릭 실패가 아니라 게임 상태에 따른 정상
  거부다.
- Air 2에서 우편함 수령, 패스/미션(이미 수령 완료 상태), 길드 업그레이드
  3회, 아레나 3회, 월드보스 결과 화면을 단계별로 확인했다.
- 이미 소진된 일일 보상은 반복 수령하지 않는다. 친구 선물은 같은 날에도 새로
  도착할 수 있으므로 실행 때마다 현재 일괄 주고받기 상태를 확인한다.
- 2026-08-09 VM 2·3 재검증에서 음수 창 좌표로 인한 성장던전 미입력을 확인했다.
  Air 1(`59938`)은 화면 내 이동 후 성장던전 5개 메뉴를 재실행했고, Air 2
  (`31126`)는 절전 해제와 창 이동 후 같은 흐름을 재실행했다.

기록된 원시 제스처는 `macro_actions.json`, `macro_steps.jsonl`에 보관한다.
