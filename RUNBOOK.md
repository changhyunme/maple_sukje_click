# Maple 숙제 매크로 실행 기록

## 단계별 재검증 규칙 (2026-09-05)

이 규칙은 실기기 재검증 전의 운영 기준이다. 기존 모든 러너에 적용이 끝난 것은 아니다.

1. 한 인스턴스의 한 동작만 실행한다. 입력 전 화면 제목, 선택 탭, 대상 버튼을 확인한다.
2. 입력 후 목표 상태를 1초 간격으로 관찰하고 연속 2회 일치해야 다음 단계로 진행한다.
   기본 관찰 제한은 12초이며 광고·전투는 해당 작업의 별도 제한을 사용한다.
3. 화면 변화량은 보조 신호다. 보상은 수령 완료 문구, 횟수 감소, 보유량 증가 등
   실제 결과로 판정한다. 버튼 소실이나 프로세스 정상 종료만으로 완료 처리하지 않는다.
4. 제한 시간 초과 시 캡처를 남기고 중단한다. 현재 화면을 재확인하기 전에는
   구매·보상·소탕 입력을 반복하지 않는다. 안전한 화면 이동도 상태 확인 후 재시도한다.
5. 미확인 항목은 완료 기록을 갱신하지 않는다. 미수령·미달성·재화 사용 보류·검증 불가를
   구분해 보고한다. 월간 출석의 초록색 보상 버튼과 핑크빈 스탬프도 별도 확인한다.
6. 한 인스턴스에서 실제 결과를 확인한 뒤 다음 인스턴스로 진행한다.

`ui_guard.wait_for_state`와 이벤트 창 진입 확인에 우선 적용했다.
보상 수령 완료 판정 및 나머지 러너는 실화면 검증이 더 필요하다.

세 인스턴스의 윈도우 PID는 실행할 때 자동 탐색한다. 독립 실행은 Codex 없이도
가능하며, Python 3와 macOS 접근성·화면 기록 권한이 필요하다.

```bash
python3 full_flow_runner.py
```

전체 흐름의 마지막에는 각 VM 절전 화면을 해제하고 메인 필드 화면을 캡처한 뒤
한 통의 완료 메일에 세 이미지를 첨부한다. 수신 주소와 Gmail SMTP 인증정보는
환경변수로만 전달한다.

```bash
export MAPLE_REPORT_TO_EMAIL="받을주소@example.com"
export MAPLE_SMTP_USERNAME="발신Gmail주소@gmail.com"
export MAPLE_SMTP_APP_PASSWORD="Gmail 앱 비밀번호"
python3 full_flow_runner.py
```

최종 JSON의 `completion_email.status`가 `sent`인지 확인한다. 메일 설정이 없는
점검 실행은 `--no-completion-email`로 명시적으로 비활성화할 수 있다.

자동 탐색이 어려운 환경에서는 현재 PID를 직접 지정할 수 있다.

```bash
python3 full_flow_runner.py --pids <AIR_PID> <AIR1_PID> <AIR2_PID>
```

개별 액션은 `homework_runner.py`, `booster_runner.py`, `shop_runner.py`,
`summon_runner.py`, `friend_runner.py`, `claim_runner.py`, `guild_arena_worldboss_runner.py`를 PID와 함께 실행한다.

`shop_runner.py`는 단독 실행 시에도 절전 화면을 먼저 해제한다. 상점 아이콘과
좌측 탭 전환은 화면 변화로 확인하고, 무료보상 수령 후 첫 카드의 `AD` 배지가
예상대로 사라졌는지 검증한다. 검증에 실패하면 다음 좌표로 진행하지 않는다.

블루스택 창이 겹치거나 음수 좌표의 다른 데스크톱으로 이동하면 다음 명령으로
사용자 디스플레이(위쪽 모니터, y=-1440..0)에 재배치한다. Air/Air 1을
위쪽에, Air 2를 아래쪽에 둔다.

```bash
python3 arrange_bluestacks.py --pids 22112 59938 31126
```

`full_flow_runner.py`는 보상 수령 흐름 후 `summon_runner.py`를 실행해 `무기 소환`과
`동료 소환`의 무료 보상만 확인한다. 활성 무료보상은 결과 레이어를 닫은 뒤
이어가며, 소환권·보석을 사용하는 `일괄 소환`은 절대 누르지 않는다.

네 번째 액션(`booster_runner.py`)은 좌측 하단 AD 버튼에서 기본 선택된 `보석 구매`
보상(500젬)을 받은 뒤 `무료 보상` 탭의 보상을 받는다. 이후 단풍잎 메뉴에서
`매일 버닝 필드 부스터 이벤트 → 사용 → Max(모두 사용) → 확인`을 실행한다.
보석 구매 보상은 사용자가 명시적으로 요청한 항목이며, 소환 보석/소환권과는 별개다.
Air2(`31126`)는 빠른사냥 기본 탭이 `무료`이고 버닝필드 이벤트 최대 수량이
4개(일반 Air/Air1은 5개 프로필)라서 별도 좌표와 초기 탭 설정을 사용한다.

상점 보상(`shop_runner.py`)은 우측 상단 장바구니에서 `일반상점` 첫 카드의
`일간 1/1 무료 보상`을 1회 받는다. 첫 카드 우상단의 cyan `AD` 배지가 없으면 이미 보상 카드를 소진한
상태로 보고 해당 카드를 건너뛰므로
유료 주간/패키지 상품을 잘못 누르지 않는다. 전환 후 무료 버튼을 최대 3회 폴링하고,
클릭 입력은 실패 시 1회 재시도하며 보상 팝업 닫기 뒤에는 1.2초 대기한다.

친구 교환(`friend_runner.py`)은 햄버거 메뉴의 `친구`에서 첫 친구 행의 왼쪽
받기와 오른쪽 주기 버튼을 각각 한 번씩 누른다. 각 버튼 뒤의 `아이템 획득`
팝업을 닫은 뒤 다음 버튼을 누르며, 두 버튼이 회색 완료 상태가 되었는지 확인한다.
`일괄 전달`(유료 포인트)은 누르지 않으며, 같은 PID는 `.friend_state.json`에
날짜를 기록해 같은 날 중복 교환하지 않는다.

현재 실행 순서는 `성장던전 → 무료/부스터/반복사냥 → 일반 상점 무료보상 → 소환 무료보상 → 친구 주고받기 →
길드/아레나/월드보스 → 우편함/패스/미션`이며, 미션 수령이 5번째 숙제 순서다.

마지막 이벤트 수령(`event_runner.py`)은 폭죽 아이콘으로 이벤트 창을 연 뒤 현재
화면의 원형 빨간 알림점을 처리한다. 왼쪽 이벤트 목록은 스크롤을 지원하므로,
한 화면만 확인하고 끝내지 않고 목록을 위로 드래그한 다음 새 이벤트 본문을 다시
탐색한다. 두 화면 연속 새 이벤트가 없거나 목록이 더 움직이지 않으면 종료한다.
직사각형 `NEW` 라벨, 청록색 바로가기, 구매 버튼, 선택형 누적 보상은 자동 수령
대상에서 제외한다. 클라이언트 업데이트 직후에는 아래쪽 목록까지 수동으로 펼쳐
좌표와 제외 항목이 그대로인지 먼저 확인한다.
현재 핑크빈 일일 미션은 원형 알림점과 진행 막대가 붙은 주황색 `보상 받기`만
수령한다. `핑크빈 교환소`는 이벤트 재화를 소비하므로 자동화 대상이 아니다.

버튼 씹힘 방지를 위해 화면 전환 뒤 기본 대기시간은 1.0초로 설정했다. 성장던전
러너는 이제 각 클릭 전후의 해당 화면 영역을 캡처해 자연 발생 애니메이션 노이즈보다
큰 시각적 전환이 확인되지 않으면 재시도 후 즉시 중단한다. 마지막 결과 확인 뒤에는
성장던전 모달을 닫고 필드 복귀까지 확인하므로 다음 액션이 모달 위에 이어지지 않는다.
성장던전은 메뉴 전환 0.8초, 소탕 결과 확인 1.2초, `+` 연속 입력 0.25초를
사용하며, 보상 팝업 닫기 뒤에도 최소 0.8초를 둔다. 절전 해제 직후에는
1.2초 대기한다.

창이 다른 가상 데스크톱의 음수 `y` 좌표로 밀려 있으면 CGEvent 클릭이
성공 로그만 남기고 실제 게임에는 도달하지 않을 수 있다. `mac_gesture.py`는
이 경우 해당 PID의 BlueStacks 창만 `(50, 50)`으로 되돌린 뒤 클릭한다.

## 재실행 검증 메모

- Air 2(`31126`)는 재실행 시 무료 보상 탭과 부스터/길드 좌표가 Air와 달라
  인스턴스별 좌표 프로필을 적용했다.
- Air 2의 성장 던전 무기 소탕은 검증 당시 입장권이 0장이라 `재화가
  부족합니다`가 표시됐다. 이는 클릭 실패가 아니라 게임 상태에 따른 정상
  거부다.
- Air 2에서 우편함 수령, 패스/미션(이미 수령 완료 상태), 길드 업그레이드
  3회, 아레나 3회, 월드보스 결과 화면을 단계별로 확인했다.
- 숙제/보상은 일일 1회만 유효하므로 이미 완료된 항목은 다시 눌러도 보상이
  늘지 않는다.
- 2026-08-09 VM 2·3 재검증에서 음수 창 좌표로 인한 성장던전 미입력을 확인했다.
  Air 1(`59938`)은 화면 내 이동 후 성장던전 5개 메뉴를 재실행했고, Air 2
  (`31126`)는 절전 해제와 창 이동 후 같은 흐름을 재실행했다.

기록된 원시 제스처는 `macro_actions.json`, `macro_steps.jsonl`에 보관한다.
